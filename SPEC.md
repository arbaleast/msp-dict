# MSPinyin Dict 项目

微软拼音用户自定义词库管理工具。

## 目标
- 读取/解析微软拼音 `.dat` 词库格式
- 支持批量导入新词汇
- 支持导出为可编辑文本格式
- 支持添加/删除词条

## 微软拼音 DAT 格式 (实测版)

经过对 `custom_phrase.dat` 的二进制分析，确认格式如下：

### 文件结构
```
0x000 - 0x0FF: 文件头 (256 字节)
0x100 - 0x3FF: 预留空间 (768 字节)
0x400 -       : 数据区开始
```

### 文件头 (0x000-0x010)
```
55 aa 88 80 02 00 60 00 55 aa 55 aa 00 20 00 00
|----magic----|-ver--|-flg-|----magic2----|hdr_sz|
```

### 词条结构 (每条 60 字节)
```
偏移   大小   内容
+0x00  4 字节 固定值 0x00000000 (结束标记可能是不同的值)
+0x04  4 字节 文本长度 (uint32 LE, 汉字数量)
+0x08  8 字节 拼音编码 (非明文)
+0x10  变长   UTF-16LE 编码的文本 + 0x0000 结尾
```

文本占用：text_len * 2 + 2 字节 (包含null终止符)

### 词条解析算法
1. 从 DATA_START (0x1400) 开始
2. 每隔 60 字节扫描一个位置
3. 检查固定值是否为 0
4. 检查文本长度是否在合理范围 (1-20)
5. 如果都满足，则解析为有效词条

### 重要发现
- 微软拼音词库是**预分配空间**的固定大小文件
- 文件大小 = 0x400 + 词条数 * 60 + 预留空间
- 不支持简单的追加操作，新词条必须插入到数据区末尾的连续空间
- 添加新词条后需要**重建文件**才能被词库识别

## 项目结构

```
mspinyin-dict/
├── README.md
├── plan.md
├── SPEC.md
├── src/
│   ├── __init__.py
│   ├── parser.py           # DAT 格式解析
│   ├── editor.py           # 词条添加/删除 (重建模式)
│   ├── exporter.py          # 导出工具
│   ├── importer.py          # 批量导入
│   ├── converter.py         # 格式转换
│   └── cli.py               # CLI 工具
└── tools/
    └── benchmark.py          # 性能测试
```

## 功能列表

- [x] 解析 DAT 文件格式
- [x] 提取所有词条
- [x] 导出为文本格式 (词条\t拼音)
- [x] 添加新词条 (需要重建文件)
- [x] 删除词条 (需要重建文件)
- [x] 批量导入文本词库 (TXT/CSV)
- [x] 与 Rime 词库互转
- [x] 重建词库文件

## TODO
- [ ] 实现词频更新
- [ ] 支持从拼音生成正确的 MSPinyin 内部编码
